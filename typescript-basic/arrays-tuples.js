"use strict";
// we shall go through arrays and tuples in this file
var arr = [1, 2, 2, 4];
let fruit = ["apple", "orange", "banana"];
fruit[0] = "mango";
console.log("an array of fruit is ", fruit);
// tuple is an array with fixed number of elements of fixed types
let wifi = ["192.168.1.1", 8080];
console.log("wifi is ", wifi[0]);
